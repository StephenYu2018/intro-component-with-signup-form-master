{
  "name": "intro-component-with-signup-form-master",
  "version": "1.0.0",
  "description": "A component that introduces a service along with a form for the user to sign-up with",
  "main": "index.js",
  "scripts": {
    "build": "npx tailwindcss -i ./styles.css -o ./dist/styles.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.2.7"
  }
}
